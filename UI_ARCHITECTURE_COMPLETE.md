# SwiftChat UI Architecture Documentation

> 完整的 UI 设计架构分析文档
> 
> 生成时间: 2025-12-06

---

## 📋 目录

1. [应用架构概览](#应用架构概览)
2. [导航结构](#导航结构)
3. [主题系统](#主题系统)
4. [屏幕详细设计](#屏幕详细设计)
5. [组件库](#组件库)
6. [样式规范](#样式规范)

---

## 1. 应用架构概览

### 技术栈
- **框架**: React Native 0.74.1
- **导航**: React Navigation (Drawer + Stack)
- **UI库**: React Native Gifted Chat
- **状态管理**: Context API (AppProvider, ThemeProvider)
- **存储**: react-native-mmkv

### 应用结构图

```
┌─────────────────────────────────────────────────────────┐
│                    App.tsx (Root)                       │
│  ┌───────────────────────────────────────────────────┐  │
│  │         ThemeProvider (主题管理)                   │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │      AppProvider (应用状态)                  │  │  │
│  │  │  ┌───────────────────────────────────────┐  │  │  │
│  │  │  │   NavigationContainer                 │  │  │  │
│  │  │  │  ┌─────────────────────────────────┐  │  │  │  │
│  │  │  │  │   Stack Navigator (主导航)      │  │  │  │  │
│  │  │  │  │  ┌───────────────────────────┐  │  │  │  │  │
│  │  │  │  │  │  Drawer Navigator        │  │  │  │  │  │
│  │  │  │  │  │  ├─ ChatScreen          │  │  │  │  │  │
│  │  │  │  │  │  ├─ SettingsScreen      │  │  │  │  │  │
│  │  │  │  │  │  ├─ MCPSettingsScreen   │  │  │  │  │  │
│  │  │  │  │  │  └─ WebFetchSettings    │  │  │  │  │  │
│  │  │  │  │  └───────────────────────────┘  │  │  │  │  │
│  │  │  │  │  ┌───────────────────────────┐  │  │  │  │  │
│  │  │  │  │  │  Modal Screens           │  │  │  │  │  │
│  │  │  │  │  │  ├─ TokenUsageScreen    │  │  │  │  │  │
│  │  │  │  │  │  └─ PromptScreen        │  │  │  │  │  │
│  │  │  │  │  └───────────────────────────┘  │  │  │  │  │
│  │  │  │  └─────────────────────────────────┘  │  │  │  │
│  │  │  └───────────────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 导航结构

### 2.1 Drawer Navigator (侧边栏导航)

**配置参数:**
```typescript
drawerStyle: {
  width: 300 (iPad/大屏) | screenWidth * 0.83 (手机)
  backgroundColor: colors.background
  borderRightWidth: 1 (Mac) | 0.3 (Android) | 0 (iOS)
  borderRightColor: colors.border
}

drawerType: 'permanent' (Mac大屏) | 'slide' (移动设备)
```

**屏幕列表:**
1. **Bedrock** (ChatScreen) - 主聊天界面
2. **Settings** (SettingsScreen) - 设置页面
3. **MCPSettings** (MCPSettingsScreen) - MCP工具配置
4. **WebFetchSettings** (WebFetchSettingsScreen) - Web抓取配置

**自定义 Drawer 内容:**
- 组件: `CustomDrawerContent.tsx`
- 功能: 会话历史列表、新建会话、删除会话

### 2.2 Stack Navigator (模态导航)

**Modal 屏幕:**
1. **TokenUsage** - Token使用统计
2. **Prompt** - 系统提示词管理

**导航方式:**
```typescript
// 从任意屏幕跳转到 Modal
navigation.navigate('TokenUsage')
navigation.navigate('Prompt', { promptId: 123 })
```

---

## 3. 主题系统

### 3.1 颜色方案 (ColorScheme)

#### Light Mode (浅色模式)
```typescript
{
  // 基础颜色
  background: '#ffffff',        // 主背景
  surface: '#f5f5f5',          // 次级表面
  text: '#000000',             // 主文本
  textSecondary: '#666666',    // 次级文本
  textTertiary: '#999999',     // 三级文本
  
  // 边框和分隔
  border: '#e0e0e0',           // 主边框
  borderLight: '#eaeaea',      // 浅边框
  chatScreenSplit: '#c7c7c7',  // 聊天分隔线
  
  // 交互颜色
  primary: '#007AFF',          // 主色调 (iOS蓝)
  primaryLight: '#E3F2FD',     // 浅主色
  accent: '#FF6B6B',           // 强调色
  
  // 状态颜色
  error: '#ff4444',            // 错误
  success: '#00C851',          // 成功
  warning: '#ffbb33',          // 警告
  info: '#33b5e5',             // 信息
  
  // 组件专用
  messageBackground: '#f2f2f2',        // 消息背景
  reasoningBackground: '#f3f3f3',      // 推理块背景
  codeBackground: '#F8F8F8',           // 代码块背景
  inputBackground: '#ffffff',          // 输入框背景
  selectedBackground: '#F5F5F5',       // 选中背景
  selectedBackgroundMac: '#ECECEC',    // Mac选中背景
}
```

#### Dark Mode (深色模式)
```typescript
{
  background: '#000000',
  surface: '#1c1c1e',
  text: '#ffffff',
  textSecondary: '#8e8e93',
  border: '#38383a',
  primary: '#0A84FF',
  messageBackground: '#1c1c1e',
  codeBackground: '#2c2c2e',
  // ... 其他深色变体
}
```

### 3.2 主题切换

**实现方式:**
- Context: `ThemeProvider` + `useTheme()` hook
- 持久化: MMKV storage
- 自动跟随系统: 支持

**使用示例:**
```typescript
const { colors, isDark, toggleTheme } = useTheme();

<View style={{ backgroundColor: colors.background }}>
  <Text style={{ color: colors.text }}>Hello</Text>
</View>
```

---

## 4. 屏幕详细设计

### 4.1 ChatScreen (主聊天界面)

**文件**: `src/chat/ChatScreen.tsx` (1208 行)

#### 布局结构
```
┌────────────────────────────────────────────┐
│  Header (导航栏)                            │
│  ├─ 左: 菜单按钮 (打开Drawer)               │
│  ├─ 中: 标题 (点击显示系统提示词)           │
│  └─ 右: 模型切换 / 停止按钮                 │
├────────────────────────────────────────────┤
│                                            │
│  Chat Messages Area (GiftedChat)          │
│  ┌──────────────────────────────────────┐ │
│  │  ┌────────────────────────────────┐  │ │
│  │  │  System Prompt (可折叠)        │  │ │
│  │  └────────────────────────────────┘  │ │
│  │  ┌────────────────────────────────┐  │ │
│  │  │  User Message                  │  │ │
│  │  │  ├─ 文本                        │  │ │
│  │  │  ├─ 图片 (可预览)              │  │ │
│  │  │  ├─ 视频 (可预览)              │  │ │
│  │  │  └─ 文档 (可预览)              │  │ │
│  │  └────────────────────────────────┘  │ │
│  │  ┌────────────────────────────────┐  │ │
│  │  │  AI Message                    │  │ │
│  │  │  ├─ Markdown 渲染              │  │ │
│  │  │  ├─ 代码块 (带复制按钮)        │  │ │
│  │  │  ├─ 表格                        │  │ │
│  │  │  ├─ LaTeX 公式                 │  │ │
│  │  │  ├─ Mermaid 图表               │  │ │
│  │  │  └─ Reasoning 块 (可折叠)      │  │ │
│  │  └────────────────────────────────┘  │ │
│  │  ...                                 │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  Scroll to Bottom Button (浮动)     │ │
│  └──────────────────────────────────────┘ │
├────────────────────────────────────────────┤
│  Input Toolbar (输入工具栏)                │
│  ┌──────────────────────────────────────┐ │
│  │  File List (已选文件列表)            │ │
│  │  ├─ 图片缩略图                        │ │
│  │  ├─ 视频缩略图                        │ │
│  │  └─ 文档图标                          │ │
│  └──────────────────────────────────────┘ │
│  ┌──────────────────────────────────────┐ │
│  │  ┌───┬──────────────────────┬─────┐  │ │
│  │  │ + │  Text Input          │Send │  │ │
│  │  └───┴──────────────────────┴─────┘  │ │
│  └──────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

#### 关键组件

**1. GiftedChat 配置**
```typescript
<GiftedChat
  messages={messages}
  onSend={onSend}
  user={{ _id: 1 }}
  renderBubble={renderBubble}           // 自定义消息气泡
  renderMessage={renderMessage}         // 自定义消息渲染
  renderMessageText={renderMessageText} // Markdown渲染
  renderInputToolbar={renderInputToolbar}
  renderActions={renderActions}         // 添加文件按钮
  renderSend={renderSend}              // 发送按钮
  scrollToBottom={true}
  maxComposerHeight={isMac ? 360 : 200}
/>
```

**2. 消息气泡样式**
```typescript
// 用户消息 (右侧)
{
  backgroundColor: colors.primary,
  borderRadius: 18,
  padding: 12,
  maxWidth: '70%',
}

// AI消息 (左侧)
{
  backgroundColor: colors.messageBackground,
  borderRadius: 18,
  padding: 12,
  maxWidth: '85%',
}
```

**3. 特殊功能**
- **长按复制**: 长按消息文本复制整段
- **代码复制**: 代码块右上角复制按钮
- **图片预览**: 点击图片全屏预览
- **滚动控制**: 自动滚动 + 手动滚动检测
- **Token计数**: 双击标题显示当前会话token使用

